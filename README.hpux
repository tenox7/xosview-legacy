

  The HP-UX port provides these meters:

    - load meter
    - cpu meter:  user | nice | system | interrupt | idle
    - mem meter:  text | used | other | free
    - swap meter: used | free
    - page meter: in | out | idle

  All of them read the pstat(2) interface, so they need no privileges.

  Building
  --------

    cp targets/hpux9 .config       # HP-UX 9, gcc 2.7
    cp targets/hpux11 .config      # HP-UX 11, aCC
    gmake

  The build needs GNU make 3.77 or newer.  The 3.75 that HP-UX 9 ships as
  /usr/local/gnu/bin/gmake has neither ?= nor target specific variables and
  will not parse the Makefile.

  HP-UX 11 notes
  --------------

  The compiler is HP aC++ (/opt/aCC), which needs -AA to compile and link
  against the standard C++ library; its default mode has neither namespaces
  nor <iostream>.  It spells -MMD +Maked and -Wno-write-strings +W829, which
  the target file passes through DEPFLAGS and NOWRITESTRINGS.

  HP-UX ships no libXpm, so the target builds with -DNO_XPM, and it keeps
  X11R6 off the default linker path.

  HP-UX 9 notes
  -------------

  The newest compiler for 9.x is gcc 2.7 with libg++ 2.7, which is a C++
  generation behind the other platforms.  hpux/hpux9/compat.h is force
  included into every translation unit to bridge the gap: it defines std
  away, since the compiler has no namespaces and the libg++ streams are in
  the global one, and it declares snprintf, vsnprintf and usleep, which
  9.x either hides from its headers or does not have at all.
  hpux/hpux9/iostream maps <iostream> onto the pre-standard <iostream.h>.

  X11R5 lives in /usr/include/X11R5 and /usr/lib/X11R5 rather than on the
  default search paths, and the linker makes a single pass over archives,
  so the target file adds those paths and repeats -lX11 after -lXpm.

  The swap meter is the one meter whose statistic moved: 9.x has no
  pss_nblksenabled and keeps the size of a block device pool and of a file
  system pool in a union instead.
